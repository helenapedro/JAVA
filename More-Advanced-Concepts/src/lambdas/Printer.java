package lambdas;

// this is a functional interface because it has a single abstract method
// An abstract method is a method that is declared without an implementation, or body.
public interface Printer {

    void print(String message);
}
